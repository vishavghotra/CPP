#include <bits/stdc++.h>
#include <stdio.h>
using namespace std;
int n, a, b, c, j, k, l;
int main()
{
    double h, a;
    scanf("%lf%lf", &h, &a);
    printf("%.8f", ((a * a - h * h) / (2 * h)));
}
